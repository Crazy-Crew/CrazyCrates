name: Release Build

on:
  workflow_dispatch:

  release:
    types: [ published ]

jobs:
  build:
    uses: Crazy-Crew/.github/.github/workflows/build.yml@main

    with:
      webhook: ${{ secrets.BUILD_WEBHOOK }}

    secrets: inherit