package com.badbones69.crazycrates.fabric.server.example.command;

/*
import com.ryderbelserion.lexicon.api.builders.commands.CommandContext;
import com.ryderbelserion.lexicon.fabric.builders.commands.FabricCommandEngine;

import java.util.Collections;

public class ExampleCommand extends FabricCommandEngine {

    public ExampleCommand() {
        super("sub", "A subcommand", "example", Collections.emptyList());

        setHidden(false);
    }

    @Override
    public void perform(CommandContext context, String[] args) {
        context.reply("This works!");
    }
}
 */