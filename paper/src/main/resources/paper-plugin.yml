name: '${name}'
main: '${group}.CrazyCrates'

loader: '${group}.LibraryLoader'

version: '${version}'
api-version: '1.20'
description: '${description}'
website: '${website}'

folia-supported: true

dependencies:
  server:
    DecentHolograms:
     load: BEFORE
     required: false
     join-classpath: true
    FancyHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    PlaceholderAPI:
     load: BEFORE
     required: false
     join-classpath: true
    ItemsAdder:
     load: BEFORE
     required: false
     join-classpath: true
    Oraxen:
     load: BEFORE
     required: false
     join-classpath: true
    CMI:
     load: BEFORE
     required: false
     join-classpath: true