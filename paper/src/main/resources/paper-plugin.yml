name: '${name}'
main: '${group}.${name}'

version: '${version}'
api-version: '${minecraft}'
description: '${description}'

authors: [ badbones69, ryderbelserion ]

folia-supported: true

dependencies:
  server:
    # Hologram plugins
    DecentHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    FancyHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    CMI:
      load: BEFORE
      required: false
      join-classpath: true

    # Placeholder plugins
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true

    # Custom item plugins
    ItemsAdder:
      load: BEFORE
      required: false
      join-classpath: true
    Oraxen:
      load: BEFORE
      required: false
      join-classpath: true
    Nexo:
      load: BEFORE
      required: false
      join-classpath: true

    # Skull plugins
    HeadDatabase:
      load: BEFORE
      required: false
      join-classpath: true

    # Cosmetic plugins
    HMCWraps:
      load: BEFORE
      required: false
      join-classpath: true

    # Other crate plugins to migrate for people
    ExcellentCrates:
      load: BEFORE
      required: false
      join-classpath: true